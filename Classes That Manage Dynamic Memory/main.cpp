#include <vector>
#include "SimpStr.h"
#include <iostream>

int main()
{
	std::vector<SimpStr> ss;
	ss.push_back(SimpStr("hi"));
	ss.push_back(SimpStr("hi"));
	ss.push_back(SimpStr("hi"));
	ss.push_back(SimpStr("hi"));
	ss.push_back(SimpStr("hi"));

}